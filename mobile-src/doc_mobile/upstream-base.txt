# The upstream nodejs/node release this source tree was built from.
#
# The first non-comment, non-empty line is the tag; subsequent lines are
# informational. This is a copy that ships with the source (read by
# tools/mobile-test/audit-test-edits.sh, which runs inside a materialized
# tree). The authoritative copy is upstream-base.txt at the root of the
# project's patches branch; scripts/prepare.sh fails if the two disagree,
# so update both when bumping upstream (see docs/UPGRADING.md).

v24.18.0
Upstream Node.js v24.18.0 (LTS, "Krypton")
