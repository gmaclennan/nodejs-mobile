# Git tree hash the reconstruction (scripts/prepare.sh) must produce.
# This is the integrity anchor: prepare = clone(upstream-base) + patches +
# mobile-src overlay, and the resulting tree must hash to exactly this.
# Update it (from prepare.sh's output) whenever patches/ or mobile-src/
# change intentionally. Current value == the tree of the mobile/v24
# materialization (upstream v24.18.0 + this branch).
6eb7e1dc0094d84ff7b932d82186fe5f72663e7d
